apiVersion: v1
kind: Service
metadata:
  name: hive-service
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: hive
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5050
